<%= bootstrap_form_with(model: hymn_book, local: true) do |form| %>
  <% if hymn_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hymn_book.errors.count, "error") %> prohibited this hymn_book from being saved:</h2>

      <ul>
      <% hymn_book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.text_field :name %>

  <%= form.submit %>
<% end %>
